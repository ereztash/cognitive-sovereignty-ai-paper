// Landing-page configuration.
const LANDING = {
  SELFCHECK_URL: "selfcheck/",   // relative path on the deployed site (see Pages workflow)
  STUDY_URL: "study/",
  REPO_URL: "https://github.com/ereztash/cognitive-sovereignty-ai-paper",
  // Free email capture: create a form at https://formspree.io and paste its ID
  // (the part after /f/). If empty, the form falls back to a mailto link if
  // CONTACT_EMAIL is set, otherwise it just shows a thank-you.
  FORMSPREE_ID: "",
  CONTACT_EMAIL: "",
};
