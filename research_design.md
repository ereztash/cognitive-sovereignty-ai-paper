# Data Notes

The original source thesis was provided as a user-authored Hebrew text. To reproduce the structural analysis, place the source file here as:

`data/source_text.txt`

The scripts will run with a small embedded placeholder if no source text is provided, but the main analysis requires the original document.
